function stringLength(one, two, three) {
    let sumLengths = one.length + two.length + three.length;
    console.log(sumLengths);
    console.log(Math.floor(sumLengths / 3))
}
stringLength('chocolate', 'ice cream', 'cake')